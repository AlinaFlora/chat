import { makeStyles, styled } from '@material-ui/core'
import { colors } from "../../config";

export const useRoomStyles = makeStyles({
  roomTitle: {
    display: 'flex',
    color: colors.font.primary,
    minWidth: '250px',
  },

})

